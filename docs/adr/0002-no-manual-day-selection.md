# No manual day selection

Rutinko will not let the user pick or change the app's current day. `New Day` only clears today's statuses, skipped tasks, snoozes and notification history while keeping routines intact, because adding manual date selection would push Rutinko toward a calendar or todo system instead of a simple daily autopilot.
